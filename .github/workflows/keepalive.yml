name: Keep app alive

on:
  # schedule:
  #   - cron: "0 */5 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: |
          curl -s -o /dev/null -w "%{http_code}" ${{ secrets.APP_URL }}
